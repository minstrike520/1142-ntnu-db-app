# === 系統配置 ===
NODE_ENV=development

# === 資料庫配置 (PostgreSQL) ===
POSTGRES_USER=chatuser
POSTGRES_PASSWORD=chatpassword
POSTGRES_DB=chatdb
DATABASE_URL=postgresql://chatuser:chatpassword@db:5432/chatdb

# === 後端配置 ===
PORT=4000
JWT_SECRET=dev_secret_key
JWT_EXPIRES_IN=15m
# 關閉速率限制（開發 / 測試用）。
# 【正式部署務必改為 false】：Compose 會讀取本檔複製而成的 .env 進行插值，
# 此處的 true 會一路帶進 docker-compose.prod.yml 的
# ${RATE_LIMIT_DISABLED}，使正式環境完全沒有速率限制。
RATE_LIMIT_DISABLED=true
# 是否無條件採信轉送標頭。僅在後端確實位於可信任的反向代理之後、且該埠
# 不可被直接連線時才設為 true；否則任何能直連後端埠的人都能偽造標頭
# 自行挑選限流桶。一般情況請改用下方的 TRUSTED_PROXY_IPS。
TRUST_PROXY=false
# 可信任代理的來源位址清單（逗號分隔，需完全相符，不接受網段）。
# 只有當 TCP 連線的對端位址出現在此清單時，後端才會採信
# CF-Connecting-IP / X-Forwarded-For 來判定使用者 IP；X-Forwarded-For
# 一律由右往左取第一個非清單內的位址（代理會把實際連線來源附加在最後，
# 左側是呼叫端自己送的內容）。
# 刻意不接受網段：容器網路的 gateway 位址就落在網段內，而它正是所有
# 主機本機請求的對端位址，填網段等於讓這些請求可自行挑選限流桶。
#
# 【刻意註解掉，請勿在此填值】docker-compose.prod.yml 已用自己的
# ${TUNNEL_IP} 帶入 cloudflared 容器的固定位址；在此填值不會覆寫正式
# compose（變數名稱不同），但會影響開發用 docker-compose.yml。
# TRUSTED_PROXY_IPS=
#
# 【同樣刻意註解掉】cloudflared 容器的固定位址與其所在網段，供
# docker-compose.prod.yml 使用。維持註解時 compose 內的 :- 預設值才會
# 生效；若此網段與主機既有網路衝突，請兩者一起解除註解並改為同一網段內
# 的位址（只改其中一個會在 docker compose up 時直接報錯）。
# TUNNEL_SUBNET=10.83.71.0/24
# TUNNEL_IP=10.83.71.250
CORS_ORIGINS=http://localhost:3000,http://localhost:3005

# === 前端配置 ===
# Next.js 前端呼叫後端位址
# Browser-facing backend URL. Docker Compose exposes backend host port 4005.
NEXT_PUBLIC_API_URL=http://localhost:4005
# 允許進行開發連線的來源網域（例如使用 Tailscale 連線時填入：raynb.tail5fc9fe.ts.net,100.73.193.72）
ALLOWED_DEV_ORIGINS=
# 訊息最大字數限制（預設為 1000）
NEXT_PUBLIC_MAX_MESSAGE_LENGTH=1000

# Docker Compose upload storage source
# Default: Docker named volume `app_uploads`
# Example host path on Windows: C:/chat-uploads
# Example host path on macOS/Linux: /Users/you/chat-uploads
UPLOADS_MOUNT_SOURCE=app_uploads

# Attachment upload configuration
# By default attachment type restriction is disabled, so uploads stay type-open.
ATTACHMENT_TYPE_RESTRICTION_ENABLED=false
# Built-in reference whitelist. These values only take effect when
# ATTACHMENT_TYPE_RESTRICTION_ENABLED=true, and can be overridden per deployment.
ATTACHMENT_ALLOWED_MIME_TYPES=image/jpeg,image/png,image/gif,application/pdf,application/zip,text/plain
ATTACHMENT_ALLOWED_EXTENSIONS=.jpg,.jpeg,.png,.gif,.pdf,.zip,.txt
# 10 MB default
ATTACHMENT_MAX_BYTES=10485760
# === 部署配置 ===
# Cloudflare Tunnel Token
TUNNEL_TOKEN=
